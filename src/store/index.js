import { createStore } from "vuex";

import auth from './modules/auth/index'

export default new createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth
  }
})
